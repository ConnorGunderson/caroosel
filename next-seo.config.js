const title = 'Caroosel: play, mix, share music';

const description = 'Flexible audio mixing app';

const SEO = {
  title,
  description,
  canonical: null,
  openGraph: {
    type: 'website',
    locale: 'en_IE',
    url: null,
    title,
    description
  }
};

export default SEO;
